#include <stdio.h>
#include "libAgenda.h"

int main ()
{
   int ano;
   char entrada;

   printf("--> Entre com o ano:\n");
   scanf("%d", &ano);

   struct agenda ag = criaAgenda(ano);

   while (entrada != 's')
   {

    if (validaData(compr -> ))    

   } 
}